package com.example.CustomerSupport.constants;

public class DBConstants {
    public enum TicketStatus {
        ACTIVE,
        INITIATED,
        INACTIVE,
        CLOSED,
        RESOLVED;
    }
}
